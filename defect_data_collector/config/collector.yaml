# Node-agnostic default for package install (Docker-safe output path)
/**:
  ros__parameters:
    topic: "/defect_gen/defect_pose"
    frame_id: "world"
    batch_size: 200
    max_flush_seconds: 2.0
    output_dir: "/root/defect_logs"
    file_basename: "defect_pose_log"
    format: "parquet"
    reliability: "best_effort"
    depth: 10
